// Define the array
var A = ["Nokia", "Samsung", "Apple", "Sony", "Huawei"];

// Printing each element on a new line using a for loop
for (var i = 0; i < A.length; i++) {
    document.write(A[i] + "<br>");
}